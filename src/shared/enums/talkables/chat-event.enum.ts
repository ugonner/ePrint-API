export enum TalkableChatEvents {
    JOIN_ROOM = "join_room",
    JOIN_AS_ADMIN = "join_as_admin",
    JOIN_ROOM_INVITE = "join_room_invite",
    JOIN_INVITE_ACCEPTANCE = "join_invite_acceptance",
    JOIN_INVITE_REJECTION = "join_invite_rejection",
    CHAT_MESSAGE = "chat_message",
    GET_CHAT_ROOMS = "get_chat_rooms"
}